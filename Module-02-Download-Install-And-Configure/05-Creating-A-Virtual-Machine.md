# Creating a Virtual Machine

A virtual machine (VM) is created using virtualization software
to run an operating system on virtualized hardware.

## Basic steps
- Create a new virtual machine
- Select the operating system type
- Allocate CPU, memory, and storage
- Attach the installation ISO
- Start the VM and begin installation

Virtual machines are widely used for testing and learning.
