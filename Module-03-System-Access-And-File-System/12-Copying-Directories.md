# Copying Directories

Directories in Linux can be copied
using the `cp` command with the recursive option.

## Copy a directory
- `cp -r source_dir destination_dir`

## Common options
- `-r` – copy directories recursively
- `-v` – show copied files (verbose)
- `-p` – preserve file permissions and timestamps

Copying directories is a common task
when backing up or duplicating data.
